import * as types from "../actions/types";

const initialState = {
  loggedInStaff: null
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
